
{
    import "Printerlib";
    include "testsuite.ycp";

    map READ = $[ "target" : $[ "tmpdir" : "/tmp/dir" ] ];

    TEST (``(Printerlib::preparePsTestPage ("lp", "/tmp/file", false)), [READ, $[], $[]], 0);
    TEST (``(Printerlib::preparePsTestPage ("xxx", "/tmp/file", true)), [READ, $[], $[]], 0);
}
