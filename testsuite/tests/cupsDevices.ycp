/**
 * File:
 *  misc.ycp
 *
 * Module:
 *  Printer configurator
 *
 * Summary:
 *  CUPS device names handling testsuite
 *
 * Authors:
 *  Jiri Srain <jsrain@suse.cz>
 *
 * $Id$
 *
 */

{
    import "Pkg";
    include "testsuite.ycp";
    import "Printerlib";

    map READ = $[
	"target" : $[
	    "size" : 5,
	],
   ];

    Printerlib::cups_usb_devices = [
	[ "EPSON", "EPSON", "usblp0" ],
	[ "CANON", "CANON", "usblp1" ],
	[ "EPSON", "EPSON", "usblp2" ],
    ];

    TEST (``(Printerlib::getProblematicCupsUsbDevices ()), [], "");

}
