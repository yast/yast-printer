/**
 * File:
 *  selectPrinter.ycp
 *
 * Module:
 *  Printer configurator
 *
 * Summary:
 *  Tests for basic translation functions.
 *
 * Authors:
 *  Petr Blahos <blahos@suse.cz>, 2001
 *
 * $Id$
 *
 */

{
    include "testsuite.ycp";
    import "Printer";

    Printer::printcap = [
	$[ "name" : "lp0", "uri" : "parallel:/dev/lp0",],
	$[ "name" : "lp1", "uri" : "parallel:/dev/lp1",],
	];
    Printer::selectPrinter (nil);
    DUMP (Printer::index);
    Printer::selectPrinter (-1);
    DUMP (Printer::index);
    Printer::selectPrinter (2);
    DUMP (Printer::index);
    Printer::selectPrinter (0);
    DUMP (Printer::index);
    Printer::selectPrinter (1);
    DUMP (Printer::index);
}
