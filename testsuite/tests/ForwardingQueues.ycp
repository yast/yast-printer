/**
 * File:
 *  ForwardingQueues.ycp
 *
 * Module:
 *  Printer configurator
 *
 * Summary:
 *  Miscelaneous tests.
 *
 * Authors:
 *  Petr Blahos <pblahos@suse.cz>
 *
 * $Id$
 *
 */

{
    include "testsuite.ycp";
    import "Printer";

    Printer::printcap = [
	$["name" : "lp", "uri" : "parallel:/dev/lp0", ],
	$["name" : "lp1", "uri" : "lpd:/dev/lp1", ],
	$["name" : "lp2", "uri" : "lpd:/dev/lp2", ],
	$["name" : "lp3", "uri" : "ipp://cups.suse.de/printers/lp", ],
	];
    DUMP (Printer::ForwardingQueues ());

    Printer::printcap = [
	$["name" : "lp", "uri" : "parallel:/dev/lp0", ],
	$["name" : "lp1", "uri" : "usb:/dev/lp1", ],
	$["name" : "lp2", "uri" : "usb:/dev/lp2", ],
	$["name" : "lp3", "uri" : "ipp://cups.suse.de/printers/lp", ],
	];
    DUMP (Printer::ForwardingQueues ());
}
