
{
    list v = SCR::Read (.vendors);
    y2milestone ("%1", v);
    foreach (`i, v, ``{
	list d = SCR::Read (add (.models, select (select (select (i, 0, nil), 0, nil), 0, "")));
	y2error ("  %1", d);
	foreach (`i, d, ``{
	    list c = SCR::Read (add (.configstree, select (select (select (i, 0, nil), 0, nil), 0, "")));
	    y2error ("    %1", c);
	});
    });
    return nil;
}
