/**
 * File:
 *   smb_queues.scr
 * Summary:
 *   SCR Agent for reading print queues from remote
 *   samba server.
 * Access:
 *   read-only
 * Authors:
 *   Petr Blahos <pblahos@suse.cz>
 * See:
 *   anyagent
 *   libscr
 * Example:
 *   Read(.smb.queues, "samba.suse.de")
 *   (["lp", "recycled", "duplex", "preprinted", ])
 *
 * $Id$
 *
 * Returns list of samba queues on remote host.
 */
.smb.queues

`ag_smb_queues ()
