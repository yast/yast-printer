/**
 * File:
 *      vendorname.ycp
 *
 * Module:
 *      Printer
 *
 * Summary:
 *      testsuite for ppd agent
 *
 * Authors:
 *      Jiri Srain <jsrain@suse.cz>
 *
 * $Id$
 *
 */

{
    y2error ("%1", SCR::Read (.db.modelname, ["HP", "DeskJet 815C"]));
    y2error ("%1", SCR::Read (.db.modelname, ["HP", "Desk-Jet 815C"]));
    y2error ("%1", SCR::Read (.db.modelname, ["HP", "DeskJet815C"]));
    y2error ("%1", SCR::Read (.db.modelname, ["HP", "DeskJet 815C series"]));
    y2error ("%1", SCR::Read (.db.modelname, ["HP", "HP DeskJet 815C"]));
    y2error ("%1", SCR::Read (.db.modelname, ["HP", "HP Desk-Jet 815C"]));
    y2error ("%1", SCR::Read (.db.modelname, ["HP", "hp DeskJet815C"]));
    y2error ("%1", SCR::Read (.db.modelname, ["HP", "hp DeskJet 815C series"]));
    y2error ("%1", SCR::Read (.db.modelname, ["HP", "HPDeskJet 815C"]));
    y2error ("%1", SCR::Read (.db.modelname, ["HP", "HPDesk-Jet 815C"]));
    y2error ("%1", SCR::Read (.db.modelname, ["HP", "hpDeskJet815C"]));
    y2error ("%1", SCR::Read (.db.modelname, ["HP", "hpDeskJet 815C series"]));

}
